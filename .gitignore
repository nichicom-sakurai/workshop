# macOS
.DS_Store

# Dependencies
node_modules/

# Terraform local runtime files
.terraform/
terraform.tfstate
terraform.tfstate.*
*.tfvars
*.tfvars.json

# Bun lockfiles are intentionally not committed yet (skeleton stage); revisit later.

# mise machine-local overrides
mise.local.toml
.mise.local.toml

# Secrets / machine-specific values (keep *.template, commit those)
*.local
.env
.env.*
!*.template
