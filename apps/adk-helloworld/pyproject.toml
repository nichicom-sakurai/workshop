[project]
name = "adk-helloworld"
version = "0.1.0"
description = "Minimal Google ADK HelloWorld agent for local adk run / adk web and Cloud Run deploy learning."
readme = "README.md"
requires-python = "==3.13.*"
dependencies = [
  "google-adk==2.2.0",
]

[tool.uv]
package = false
