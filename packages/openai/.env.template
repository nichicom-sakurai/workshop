# packages/openai 用の環境変数テンプレート。
# このファイルを .env にコピーして値を設定してください (.env は gitignore 済み)。
# Bun は実行時に同ディレクトリの .env を自動読み込みします。

# OpenAI API key (必須)。https://platform.openai.com/api-keys で発行。
OPENAI_API_KEY=

# 任意: agent の model を明示指定 (未設定なら SDK 既定の model)。例:
# OPENAI_DEFAULT_MODEL=gpt-5
